import modulo

# from modulo import div - Serve para importar apenas uma parte do módulo. QUando fazemos isso, não precisamos usar o nome
# do módulo antes -  print(div(a,b))

print(modulo.div(10, 2))
print(modulo.sub(10, 3))
print(modulo.mult(15, 25))
print(modulo.sum(20, 36))
